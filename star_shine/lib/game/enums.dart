enum GameLevel { easy, normal, hard }
enum GameState {
  initial,
  playing,
  paused,
  gameOver, ready,
}
enum TileSpeed { slow, normal, fast }
enum TileStatus {
  active,
  hit,
  missed,
  // add other states as needed
}
